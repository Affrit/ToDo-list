import { FC, FunctionComponent, JSXElementConstructor, ReactComponentElement, ReactElement } from "react"
import { SignInPage } from "../components/SignInPage/SignInPage"
import { TodoPage } from "../components/TodoPage/TodoPage"

/*
export interface IRoute {
  path: string;
  element: ReactElement<any, string | JSXElementConstructor<any>> | null | undefined | FC;
}
*/

/* export enum RouteNames {
  LOGIN = '/login',
  TODO = '/'
}

export const publicRoutes: IRoute[] = [
  {path: RouteNames.LOGIN, element: SignInPage}
]

export const privateRoutes: IRoute[] = [
  {path: RouteNames.TODO, element: TodoPage}
] */
